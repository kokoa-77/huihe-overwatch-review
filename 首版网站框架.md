# 守望先锋 2 训练记录与复盘网站 · 首版框架

状态：实现依据。项目需要以开源仓库发布到用户自己的 GitHub；本地首版为虚构数据驱动的可交互演示，尚未连接云服务。

当前交付范围：可操作的静态演示骨架已实现对局新增与编辑、按地图 / 英雄 / 结果 / 复盘状态筛选、结算数据记录、结构化复盘事件与阵亡判断、训练目标逐场自评、英雄 / 地图趋势筛选，以及本地 JSON 备份。旧版浏览器记录会在读取时补齐新字段。账号、真实分享、云端存储与跨设备同步仍属于后续阶段。

## 1. 产品目标与边界

面向所有位置、段位的玩家：记录对局 → 找到需要复盘的场次 → 写下关键事件 → 定下一次训练动作 → 看自己的变化。

- 免费使用、代码开源是产品目标；代码最终发布在用户自己的 GitHub 仓库，仓库设为公开，并附带开源许可证。部署成本以实际上线方案和额度为准，不承诺永久零成本。
- 正式版从一开始具备账号、云端私密记录和跨设备同步。
- 记录默认只有本人可见；用户主动分享单份复盘，可撤销；不做公开广场、排行榜。
- 新项目独立建设，不读取、修改或复制旧训练项目的个人数据、Notion 链接及组件。
- 演示数据全部虚构，明显标注“演示模式”，与真实账号数据隔离。

## 2. 页面与导航

| 页面 | 路由 | 首版内容 |
|---|---|---|
| 欢迎 / 登录 | `/`、`/login` | 产品说明、进入演示、注册 / 登录 |
| 今日训练 | `/app` | 今日一个训练目标、快速记一局、待复盘对局、近期概况 |
| 对局记录 | `/app/matches` | 时间 / 英雄 / 地图 / 结果筛选，新建编辑、删除、进入复盘 |
| 单局复盘 | `/app/matches/:id` | 基本战绩、回放码、外部视频链接、事件时间点、总结、分享 |
| 训练计划 | `/app/training` | 一个当前目标、可观察的达成标准、截止日期、逐场执行记录 |
| 我的趋势 | `/app/trends` | 场次、胜负平、每 10 分钟阵亡、复盘完成数、目标做到率 |
| 设置与数据 | `/app/settings` | 昵称、位置 / 英雄偏好、JSON 导入导出、分享管理、退出登录 |
| 分享查看 | `/s/:token` | 单份复盘的只读快照；失效 / 撤销状态 |

桌面端固定左侧导航，主内容区统一顶栏；手机端底部导航“今日 / 对局 / 训练 / 趋势”，设置放账号菜单，复盘作为详情页。

## 3. 页面布局示意

```text
┌──────────┬──────────────────────────────────────────────┐
│ 网站名称 │ 今日训练              保存状态   ＋记一局  账号 │
│          ├──────────────────────────────────────────────┤
│ 今日训练 │ 今日只练一件事                                │
│ 对局记录 │ 目标内容 · 达成标准                 [开始记录] │
│ 训练计划 ├──────────────┬──────────────┬────────────────┤
│ 我的趋势 │ 近期场次     │ 已完成复盘   │ 训练做到率     │
│          ├───────────────────────────┬──────────────────┤
│          │ 最近对局                  │ 待复盘           │
│          │ 地图 / 英雄 / 结果 / 时间 │ 尚未写总结的对局 │
│ 设置数据 │ 点击进入单局复盘          │ [继续复盘]       │
└──────────┴───────────────────────────┴──────────────────┘

单局复盘：
┌───────────────────────────────────────────────────────┐
│ 返回对局   地图 · 英雄 · 胜负 · 日期     保存 / 分享     │
├─────────────────────────────┬─────────────────────────┤
│ 战绩摘要                    │ 回放码 [复制]           │
│ 事件时间线                  │ 外部视频链接 [打开]     │
│ 03:20 发生了什么             │                         │
│       我的判断 / 下次动作    │ 本局一个最重要的问题    │
│ [添加时间点]                │ 下一局准备做什么        │
└─────────────────────────────┴─────────────────────────┘
```

视觉建议：深灰蓝底、橙色主按钮、清晰中文排版；胜负同时用文字和颜色表达。图表与装饰服务于复盘，不挤占填写空间。手机端两栏自然改成单栏。

## 4. 最小可用流程

1. 注册 / 登录，选位置和常用英雄；偏好允许跳过。
2. 快速记一局：时间、地图、英雄（可多选）、胜负平；时长、阵亡、段位、回放码均可后补。
3. 写复盘：一个问题、一个下次动作；可加多个时间点，每条分别写“观察事实”和“个人判断”。
4. 把下次动作设为训练目标，在后续对局标记“做到 / 未做到 / 未评估”。
5. 在另一台设备登录可读取已保存记录；查看筛选后的趋势。
6. 分享前预览字段，生成只读链接；撤销后通过该链接再次访问不可读取。

保存按钮显示保存中 / 已保存 / 失败重试。云端写入成功才显示已保存；网络失败保留当前输入。同一记录被其他设备修改时提示重新载入或保留草稿，避免静默覆盖。

## 5. 技术骨架

首版采用浏览器原生 HTML、CSS、JavaScript，无构建步骤和外部运行时依赖，打开 `index.html` 即可体验，也能直接部署到 GitHub Pages。数据操作单独放在 repository 模块中，云端版本再替换为正式数据实现；届时可使用 React + TypeScript + Vite 前端及 Supabase Auth + PostgreSQL，具体按接口和部署实测确定。

```text
前端页面与公共组件
        ↓
数据访问接口（账号 / 对局 / 复盘 / 训练 / 分享）
        ├─ 演示实现：虚构数据、本机演示存储
        └─ 正式实现：Supabase 登录 + 数据库行级权限
                                     ↓
                          服务端分享接口 / 后续导入适配器
```

建议目录：

```text
src/
  app/             路由、整体布局、登录状态
  pages/           页面
  components/      表单、卡片、时间线、空状态
  features/        matches、reviews、training、trends、sharing
  data/            repository 接口、demo 与 supabase 实现
  domain/          数据类型、字段校验、统计纯函数
  demo/            虚构样例
supabase/
  migrations/      表结构、权限策略
  functions/       创建 / 读取 / 撤销分享
```

前端构建参考 [Vite 官方指南](https://vite.dev/guide/)。账号间隔离在数据库执行，所有个人表启用 RLS 并设置最小授权；服务端高权限密钥不可进入浏览器。[Supabase 权限文档](https://supabase.com/docs/guides/database/postgres/row-level-security)

## 6. 最小数据模型

| 表 | 核心字段 |
|---|---|
| profiles | id（账号 ID）、nickname、preferred_roles、preferred_heroes |
| matches | id、user_id、played_at、map、mode、heroes[]、result、duration_seconds?、deaths?、rank?、replay_code?、video_url?、source、external_id?、version、created_at、updated_at |
| reviews | id、user_id、match_id（每局一份）、summary、next_action、completed_at? |
| review_events | id、user_id、review_id、time_seconds、category、observation、interpretation、next_action |
| training_goals | id、user_id、title、success_criterion、due_at?、status |
| goal_checks | id、user_id、goal_id、match_id、result（做到 / 未做到 / 未评估） |
| share_links | id、user_id、review_id、token_hash、snapshot、expires_at?、revoked_at? |

个人表只允许本人增删改查，父子记录必须属于同一账号。每个目标与每场对局最多一条执行记录。

分享走服务端：检查所有者 → 提取用户勾选的字段 → 保存快照 → 生成足够随机的令牌。读取时验证令牌、有效期和撤销状态，只返回快照；不开放原始个人表。复盘后续修改不自动更新已分享快照，可撤销重建。界面说明“获得链接的人可以查看”；撤销不能收回别人已保存的副本。

## 7. 统计与导入规则

- 胜率 = 胜场 / 总场次，平局计入总场次，同时显示胜负平与样本数。
- 每 10 分钟阵亡 = 有效记录的总阵亡 / 同一批记录总秒数 × 600。缺时长或阵亡的记录不参与，显示有效样本数；无数据显示“暂无数据”。
- 训练做到率 = 做到 /（做到 + 未做到），未评估不进分母。
- 多英雄对局按“包含该英雄的对局”统计，提示各英雄场次不可直接相加。
- 图表只能说明记录的变化；不从计分板直接推断死亡原因或输赢因果。
- JSON 备份带 schemaVersion；导入先校验与预览，归属当前账号，不信任文件中的 user_id。重复 ID 提示跳过，不静默覆盖，不导入登录凭据与分享令牌。

## 8. 首版与后续分界

首版完成：演示体验、真实账号、私密云记录、手动对局、时间点复盘、训练目标、基础趋势、可撤销分享、JSON 备份。

后续验证：网易大神等战绩来源的授权与稳定性，接入独立 import adapter；统一转换成 matches 数据，并以账号 + 来源 + 外部 ID 去重。

回放码首版只保存、复制并提示去游戏内观看。外部视频首版保存和打开链接；不实现回放码直接播放、不托管视频、不承诺读取游戏进程。AI 自动分析不作为首版依赖。

## 9. Luna 的实现顺序与验收

1. **先出可点击演示骨架**：路由、桌面 / 手机布局、虚构数据、记一局表单、复盘页。此阶段标注演示，不能宣称云同步完成。
2. **再接真实账号与云存储**：迁移、权限、增删改查、错误与保存状态。未配置后端时仍可体验演示。
3. **补齐复盘闭环**：训练目标、趋势计算、JSON 导入导出、分享生成与撤销。
4. **交付可运行仓库**：README、本机启动和部署说明、.env.example、开源许可证（建议 MIT），不包含真实凭据与个人数据。

必要验收：演示可用；两账号互相不能读写数据（包含直接调用接口）；同账号跨设备读取；分享只暴露勾选字段且撤销生效；保存失败不显示成功；零数据 / 缺时长 / 平局 / 多英雄统计正确；手机无横向溢出。正式上线前验证实际注册渠道和国内访问情况。

可直接给 Luna 的任务：

> 按当前项目《首版网站框架.md》实现。先完成第 9 节第一步，交付能运行、能点击、能新增对局并写复盘的演示骨架，给我看实际页面，再继续云端功能。保持后端数据接口可替换，演示模式明确标注。只操作当前新项目，使用虚构样例，不读取旧训练项目。不要做排行榜、公开广场、回放码网页播放或 AI 分析。以这份框架作为范围，具体小选择自行判断，优先尽快给我可体验的版本。
